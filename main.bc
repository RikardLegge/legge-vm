double :: fn(i: int) -> int {
    return 2 * i;
}
exec :: fn(num: int, map: Fn(int) -> int) -> int {
    return map(num);
}
exec(2, double);

import log;
import int;
import float;

a :: float(1)/2.0;
log(a);
